version: '3.8'

services:
  # CashFlow Telegram Bot
  cashflow-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: cashflow-bot:latest
    container_name: cashflow_bot
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    env_file:
      - .env
