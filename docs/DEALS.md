# Модуль сделок - Deals Module

## Назначение и архитектура модуля

Модуль сделок отвечает за генерацию случайных инвестиционных возможностей в игре Cash Flow. Состоит из четырех основных компонентов:

- **smallDeals.js** (`src/game/cards/smallDeals.js`) - массив мелких сделок с расширенными параметрами
- **bigDeals.js** (`src/game/cards/bigDeals.js`) - массив крупных сделок с расширенными параметрами
- **deals.js** (`src/game/deals.js`) - генераторы сделок и вспомогательные функции
- **dealCirculation.js** (`src/utils/dealCirculation.js`) - утилита для циркуляции специальных карт между игроками

Модуль предоставляет:
- Генерацию мелких сделок с богатыми параметрами
- Генерацию крупных сделок с расчетом кредитования
- Случайный выбор из предопределенных массивов
- Расчет финансовых параметров сделок

Архитектурно модуль:
- Экспортирует функции генерации из разных файлов
- Содержит статические массивы предопределенных сделок
- Возвращает объекты сделок со всеми необходимыми полями
- Не зависит от других модулей игры

## Структура мелкой сделки (smallDeals)

Мелкие сделки из `smallDeals.js` имеют расширенную структуру:

```javascript
{
  id: "small_deal_0",           // Уникальный идентификатор
  type: "small",                // Тип сделки
  title: "string",              // Название сделки
  description: "string",        // Подробное описание
  cost: number,                 // Стоимость в рублях (может отсутствовать)
  passiveIncome: number,        // Пассивный доход в рублях (может отсутствовать)
  roi: "string",                // Процент доходности (например, "38%")
  sellRange: "string",          // Диапазон продажи (например, "1.4 млн ₽ - 1.9 млн ₽")
  range: "string",              // Диапазон цен для акций (например, "150 ₽ - 1 200 ₽")
  downPayment: number,          // Первоначальный взнос (может отсутствовать)
  mortgage: number,             // Ипотечный кредит (может отсутствовать)
  canSellToOthers: boolean,     // Можно ли предлагать другим игрокам
  unlimitedStocks: boolean,     // Неограниченное количество акций
  canSellStocks: boolean,       // Можно ли продавать акции
  multiple: number,             // Множитель для акций (увеличение/уменьшение)
  group_Id: string,             // ID группы активов (для anyCanBuySell)
  anyCanBuySell: boolean        // Карта циркулирует между игроками с активами группы
}
```

## Структура крупной сделки (bigDeals)

Крупные сделки из `bigDeals.js` имеют расширенную структуру:

```javascript
{
  id: "big_deal_0",             // Уникальный идентификатор
  type: "big",                  // Тип сделки
  title: "string",              // Название сделки
  description: "string",        // Подробное описание
  cost: number,                 // Стоимость в рублях
  passiveIncome: number,        // Пассивный доход в рублях (месячный)
  mortgage: number,             // Сумма ипотечного кредита
  downPayment: number,          // Первоначальный взнос
  roi: "string",                // Процент доходности (например, "48% КОИ")
  sellRange: "string",          // Диапазон продажи (например, "1.5 млн ₽ - 2.4 млн ₽")
  canSellToOthers: boolean,     // Можно ли предлагать другим игрокам
  apartments: number            // Количество квартир (для жилых комплексов)
}
```

## Информация на карточке сделки

Карточка сделки отображает следующую информацию для принятия решения:

- **Название и описание** сделки
- **Стоимость** (если указана)
- **Денежный поток/пассивный доход** (ежемесячный доход)
- **Процент доходности** (ROI)
- **Диапазон продажи** (для недвижимости)
- **Диапазон цен** (для акций)
- **Первоначальный взнос** (если указан)
- **Ипотека** (если указана)
- **Ваши деньги** - текущий баланс игрока
- **Стоимость кредитной карты** - 2% от стоимости (альтернативный способ оплаты)

## Логика выдачи карт с исключением дубликатов

Система предотвращает повторную выдачу одних и тех же карт в рамках одной игры. После выдачи карты её ID добавляется в массив использованных. Когда все карты выданы, массив очищается и цикл начинается заново.

### Механизм работы

1. **Хранение использованных карт**:
   - `usedBigDealIds`: массив ID выданных крупных сделок
   - `usedSmallDealIds`: массив ID выданных мелких сделок

2. **Выбор карты**:
   - Фильтруются доступные карты (исключая использованные ID)
   - Выбирается случайная карта из доступных
   - ID добавляется в массив использованных

3. **Сброс цикла**:
   - Когда фильтр возвращает пустой массив (все карты выданы)
   - Массив использованных очищается
   - Выбирается новая случайная карта из полного набора

### Схема данных игры

В коллекцию `games` добавлены поля:
- `usedBigDealIds`: `Array<string>` - ID выданных крупных сделок
- `usedSmallDealIds`: `Array<string>` - ID выданных мелких сделок

## Функции генерации

Карты генерируются в `handleDealType` обработчике с использованием фильтрации:

### Выбор мелкой сделки
- **Расположение**: `src/handlers/deals.js` (handleDealType)
- **Назначение**: Генерирует уникальную мелкую сделку для игры
- **Параметры**: gameId, тип 'small'
- **Возвращает**: Object - объект мелкой сделки
- **Функционал**:
  - Фильтрует smallDeals по usedSmallDealIds
  - Если доступных нет - очищает usedSmallDealIds, фильтрует заново
  - Выбирает случайную из доступных, добавляет ID в used

### Выбор крупной сделки
- **Расположение**: `src/handlers/deals.js` (handleDealType)
- **Назначение**: Генерирует уникальную крупную сделку для игры
- **Параметры**: gameId, тип 'big'
- **Возвращает**: Object - объект крупной сделки
- **Функционал**:
  - Фильтрует bigDeals по usedBigDealIds
  - Если доступных нет - очищает usedBigDealIds, фильтрует заново
  - Выбирает случайную из доступных, добавляет ID в used

## Типы мелких сделок

Мелкие сделки включают различные категории активов:

- **Недвижимость**: Квартиры, дома с указанием ROI и диапазона продажи
- **Акции**: Компаний с диапазоном цен и возможностью неограниченной покупки
- **Депозиты**: Банковские сертификаты с гарантированным доходом
- **Бизнес**: Создание компаний, франшизы
- **Золото**: Монеты с диапазоном цен продажи
- **Специальные**: Кредиты, страховки, другие финансовые инструменты

## Правила игры для сделок

### Мелкие сделки
- **Стоимость**: Полная оплата наличными (если указана)
- **Условие покупки**: `player.cash >= deal.cost` (если cost указан)
- **Результат**: Добавление актива, увеличение пассивного дохода
- **Дополнительные параметры**: Отображаются на карточке для информации

### Крупные сделки

Крупные сделки делятся на два типа в зависимости от наличия поля `mortgage`:

#### Сделки с кредитом (недвижимость, бизнесы)
- **Первоначальный взнос**: Используется значение из `downPayment` сделки
- **Кредит**: Используется значение из `mortgage` сделки
- **Ежемесячный платеж**: 0.01% от стоимости сделки
- **Условие покупки**: `player.cash >= deal.downPayment`
- **Результат**: Добавление актива + кредита, увеличение пассивного дохода и расходов

#### Сделки без кредита (партнерства, инвестиции)
- **Стоимость**: Полная оплата за `cost` сделки
- **Условие покупки**: `player.cash >= deal.cost`
- **Результат**: Добавление актива, увеличение пассивного дохода (без кредита и расходов)

## Логика циркуляции карт anyCanBuySell

Карты с флагом `anyCanBuySell` и полем `group_Id` имеют специальную логику циркуляции между игроками.

### Механизм работы

1. **Инициализация циркуляции**:
   - При генерации карты с `anyCanBuySell` и `group_Id` система находит всех игроков, владеющих активами с соответствующим `group_Id`
   - Создается список игроков `dealCirculationPlayers` и устанавливается индекс `dealCirculationIndex = 0`

2. **Циркуляция между игроками**:
   - Карта показывается игрокам из списка по очереди
   - Каждый игрок может купить/продать актив или пропустить
   - При пропуске карта переходит к следующему игроку из списка
   - При покупке/продаже циркуляция завершается, карта исчезает

3. **Завершение циркуляции**:
   - Когда все игроки из списка совершили действия, или когда кто-то купил/продал
   - `currentDeal` очищается, игра продолжается в обычном режиме

### Схема данных игры

Для поддержки циркуляции в схему игры добавлены поля:
- `dealCirculationPlayers`: массив `userId` игроков для циркуляции
- `dealCirculationIndex`: текущий индекс в массиве циркуляции

### Примеры использования

- **Акции компаний**: Карты акций циркулируют между игроками, уже владеющими акциями этой компании
- **Депозитные сертификаты**: Могут циркулировать по всем игрокам (без `group_Id`)

### Утилита dealCirculation.js

Модуль `src/utils/dealCirculation.js` предоставляет функции:
- `initializeDealCirculation()` - инициализация списка игроков
- `processDealAction()` - обработка действия игрока
- `circulateToNextPlayer()` - переход к следующему игроку
- `endDealCirculation()` - завершение циркуляции

## Интеграция с игрой

Модуль сделок используется в следующих сценариях:
- **Поле "Сделки"**: При попадании на поле DEAL
- **Выбор типа**: Игрок выбирает мелкую или крупную сделку
- **Показ карточки**: Отображение деталей сделки с кнопками действий
- **Покупка**: Финансовая транзакция и обновление состояния игрока
- **Пропуск**: Переход хода следующему игроку или циркуляция для `anyCanBuySell`

### Обработчики действий

Обработка сделок реализована в модуле `src/handlers/deals.js`:
- `handleDealType()` - выбор типа сделки и инициализация циркуляции
- `handleBuyDeal()` - покупка сделки с поддержкой циркуляции
- `handleSellStocks()` - продажа акций с поддержкой циркуляции
- `handleSkipDeal()` - пропуск с циркуляцией для специальных карт

### Сервисы и утилиты

- **DatabaseService**: Управление состоянием игры, включая поля циркуляции
- **GameService**: Бизнес-логика обработки сделок
- **MessageService**: Формирование и отправка карточек сделок
- **dealCirculation.js**: Утилита для логики циркуляции `anyCanBuySell`

### Схема данных игры

В коллекцию `games` добавлены поля для поддержки циркуляции:
- `dealCirculationPlayers`: `Array<string>` - список userId игроков
- `dealCirculationIndex`: `number` - текущий индекс в списке

## Логика предложения сделок другим игрокам

### Назначение и механизм

Механизм предложения сделок позволяет игрокам предлагать другим игрокам сделки с комиссией. Эта функция доступна только для сделок с флагом `canSellToOthers: true`.

### Состояния предложения сделки

Предложение сделки проходит через несколько состояний:

```javascript
{
  step: 'commission' | 'select_user' | 'confirmed',  // Текущий шаг предложения
  offeringUserId: string,                            // ID предлагающего игрока
  commission: number,                                // Выбранная комиссия (%)
  targetUserId: string                               // ID выбранного игрока
}
```

### Схема данных игры

В коллекцию `games` добавлено поле для поддержки предложений:
- `offerState`: `Object|null` - состояние активного предложения сделки

### Процесс предложения

1. **Инициализация предложения**:
   - Игрок нажимает "Предложить другому" на карточке сделки
   - Создается `offerState` с шагом 'commission'
   - Клавиатура заменяется на выбор комиссии

2. **Выбор комиссии**:
   - Игрок выбирает комиссию: 1%, 3%, 5%, 10%, 15%, 20%
   - Состояние обновляется на шаг 'select_user'
   - Клавиатура заменяется на выбор игрока

3. **Выбор игрока**:
   - Игрок выбирает из списка других участников игры
   - Состояние обновляется на шаг 'confirmed'
   - Создается новое сообщение со сделкой для выбранного игрока

4. **Обработка предложения**:
   - Выбранный игрок видит сделку с повышенной стоимостью
   - При покупке комиссия рассчитывается и переводится предлагающему
   - После покупки/пропуска игра продолжается нормально

### Расчёт стоимости с комиссией

Стоимость сделки увеличивается на процент комиссии:

```javascript
// Для сделок с cost
newCost = originalCost * (1 + commission/100)

// Для сделок с downPayment
newDownPayment = originalDownPayment * (1 + commission/100)

// Комиссия для перевода
commissionAmount = (downPayment || cost) * (commission/100)
```

### Передача комиссии

- Комиссия рассчитывается от оригинальной стоимости сделки
- При успешной покупке комиссия переводится на баланс предлагающего игрока
- Перевод происходит через `databaseService.updatePlayerCash()`

### Управление ходом игры

- После предложения сделки другому игроку ход переходит к следующему игроку
- Предложение очищается после выбора пользователя
- Предложенная сделка обрабатывается как обычная сделка для получателя

### Интеграция с существующей логикой

Механизм интегрирован с существующей архитектурой:
- Использует те же методы покупки сделок
- Совместим с кредитными картами и другими механиками
- Не влияет на циркуляцию `anyCanBuySell` карт

### Утилита dealOffer.js

Модуль `src/utils/dealOffer.js` предоставляет функции:
- `initializeDealOffer()` - инициализация предложения
- `processOfferStep()` - обработка шагов предложения
- `calculateDealWithCommission()` - расчёт стоимости с комиссией
- `calculateCommission()` - расчёт суммы комиссии
- `transferCommission()` - перевод комиссии
- `passTurnAfterOffer()` - передача хода после предложения

### Обновление клавиатур и сообщений

- `generateDealKeyboard()` - поддержка состояний предложения
- `generateDealCardContent()` - динамический текст в зависимости от состояния
- Новые callback'и: `select_commission_*`, `select_user_*`, `cancel_offer`

## Примеры использования

### Предложение сделки с 5% комиссией

1. Игрок А получает сделку "Квартира - 2/1" (стоимость 1.65 млн ₽)
2. Нажимает "Предложить другому"
3. Выбирает комиссию 5%
4. Выбирает игрока Б
5. Игрок Б получает сообщение со сделкой стоимостью 1.7325 млн ₽ (1.65 млн * 1.05)
6. Если игрок Б покупает сделку, игрок А получает комиссию 82,500 ₽ (1.65 млн * 0.05)
7. Игра продолжается с игроком, следующим за игроком А

### Особенности расчёта комиссии

- Комиссия всегда рассчитывается от **оригинальной** стоимости сделки
- Для сделок с первоначальным взносом комиссия применяется к взносу
- Комиссия переводится только при успешной покупке
- Предложение не влияет на обычную логику покупки (кредитные карты, etc.)

## Архитектурные принципы

- **Изоляция данных**: Сделки генерируются независимо от состояния игры
- **Предопределенность**: Все возможные сделки описаны в коде
- **Случайность**: Равномерное распределение при выборе сделок
- **Расширяемость**: Легко добавить новые сделки в массивы
- **Читаемость**: Ясная структура объектов сделок
- **Информационная полнота**: Карточки содержат все необходимые данные для принятия решений
- **Модульность**: Логика предложения вынесена в отдельный утилитарный модуль
